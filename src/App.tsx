import type { Component } from "solid-js";

const App: Component = () => {
  return (
    <div class="h-full bg-zinc-900 text-zinc-100 flex items-center justify-center">
      <p class="text-zinc-400">kusa — Markdown Reader</p>
    </div>
  );
};

export default App;
